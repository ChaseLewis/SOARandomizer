{
  "permissions": {
    "allow": [
      "WebFetch(domain:guts-dev.com)"
    ],
    "deny": [
      "Bash(git push:*)",
      "Bash(gh pr create:*)",
      "Bash(gh pr merge:*)",
      "Bash(gh release create:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "in=$(cat); if printf '%s' \"$in\" | grep -qiE 'git[[:space:]]+push|gh[[:space:]]+(pr|release)[[:space:]]+(create|merge)'; then printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Remote publish blocked: no git push / gh pr|release create|merge.\"}}'; fi"
          }
        ]
      }
    ]
  }
}
